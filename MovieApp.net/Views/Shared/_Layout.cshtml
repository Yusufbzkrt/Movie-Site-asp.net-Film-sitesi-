<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link href="~/css/Style.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        body{
            background-color: #fbfafa;
        }
    </style>
</head>
<body>
    @RenderSection("message",false)

    @await Html.PartialAsync("_navbar")

    @RenderSection("header",false)<!-- layout içine yazarız fakat her sayfada görünmesini istemeyiz bunun için bu özelliği kullanırız. ilk parametre seçmek istediğimiz sayfa ikinci parametre ise bunun her sayfa için zorunlu olmadığını elirtmek için false yazdık. -->
    <div class="container mt-3">
        @if (IsSectionDefined("menu"))
        {
            <div class="row">
                <div class="col-md-3">
                    @RenderSection("menu",false)
                </div>
                <div class="col-md-9">
                    @RenderBody() <!-- değişecek olan kısmı böyle belirtiriz. -->
                </div>
            </div>
        }
        else
        {
            <div class="row">
                <div class="col-md-12">
                    @RenderBody()
                </div>
            </div>
        }

    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
   
    @RenderSection("script", required: false)
</body>
</html>
<!--                 NOTLAR                                     -->

<!--eğer farklı tasarımlı layoutlar kullanmak istiyorsak farklı bir layout sayfası oluştururuz ve burda nerde
    değişişiklik yapılacaksa yapılır ardından
@*@{
           Layout = "_Layout2";
      } *@
  gibi bir kod bloğu değişik olmasını istediğimiz sayfaya aktarılır ve bu layout içerisindeki özellikleri kullanabilir.
-->

